export const enum PageRoutes {
  MAIN = '/',
  LOGIN = '/login',
  REGISTRATION = '/registration',
  CATALOG = '/catalog',
  NOT_FOUND = '*',
}

export enum Countries {
  RUSSIA = 'Russia',
  USA = 'USA',
}
